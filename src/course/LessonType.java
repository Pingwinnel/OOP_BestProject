/**
 * @author Yerasyil
 * @version 25.12.2023
 */
package course;
// the `LessonType enumeration represents different types of lessons, such as lectures, practices, and labs. The
// constants defined in the enumeration are `LECTURE`, `PRACTICE`, and `LAB`.
public enum LessonType {
	LECTURE, PRACTICE, LAB
}
